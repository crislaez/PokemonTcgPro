import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.pokemonTcgInfoProApp',
  appName: 'pokemon Tcg Info Pro',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
