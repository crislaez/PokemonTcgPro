import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.PokeTCGdex',
  appName: 'PokeTCGdex',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
